export function updateNode(node, htmlNode, srcString)
{
    //check all the child nodes and whitespace for changes

    let children = node.children;

    children.forEach(child => 
    {

    });

    return htmlNode;
}